# arguments: block

$setblock ~-1 ~ ~ $(block) strict
$setblock ~ ~-1 ~ $(block) strict
$setblock ~ ~ ~-1 $(block) strict
$setblock ~ ~ ~ $(block) strict
$setblock ~ ~ ~1 $(block) strict
$setblock ~ ~1 ~ $(block) strict
$setblock ~1 ~ ~ $(block) strict
